# Executor brief — B1 (scan + command shell)
North Star: house-style reuse, found-first UX, headless guarded. Fork hygiene: additive only.
Worktree /tmp/oh-my-pi-onboard-ui (branch onboard-ui). Commit NOTHING.

## Task 1: packages/coding-agent/src/modes/setup-wizard/onboard/scan.ts
PURE-DATA module: NO imports from pi-tui or anything rendering. Exports:
- types: ScanEntry { provider: string; status: 'ready'|'candidate'|'missing'; origin: {kind: string; detail: string} | null; defaultModel: string | null }
- scanProviders(deps): ScanEntry[] — deps injected for tests:
    { registry: readonly {id:string}[] (or PROVIDER_REGISTRY equivalent), auth: {hasAuth(id), getCredentialOrigin(id)}, envApiKeyName(id), homeDir: string, modelsYmlProviders?: string[] }
  Logic per provider: ready if auth.hasAuth OR env var set OR models.yml entry; candidate if
  foreign CLI store present but not wired (~/.codex/auth.json -> openai-codex,
  ~/.grok/auth.json -> grok); else missing. Rank: Arnold routes first
  [deepseek, openrouter, xai, anthropic, kimi-code, zai, moonshot, fireworks, openai-codex, grok]
  then remaining registry order. NEVER include secret values — origins/names only.
  Foreign-store checks are private functions using node:path/node:fs existsSync on
  path.join(homeDir, '.codex', 'auth.json') etc.
Default export convenience scanProvidersLive() constructing deps from real singletons
(AuthStorage instance access pattern: study how commands/setup.ts and /providers lazy.ts reach
authStorage WITHOUT a full session; if a standalone construction exists use it, else follow
model-registry.ts patterns).

## Task 2: packages/coding-agent/src/commands/onboard.ts + registration
Command subclass mirroring setup.ts pattern. Behavior:
- If NOT interactive (reuse the same checks startup-splash uses: isInteractive etc.) OR stdout
  not TTY: print exactly one line: "omp onboard needs an interactive terminal." to stderr,
  exit code 2 — BEFORE importing any wizard/TUI module (lazy imports only inside the
  interactive branch). Add a test asserting 'pi-tui' absent from require cache after a
  non-TTY invocation.
- Interactive branch: lazy-import runSetupWizard + onboard scenes (placeholder scene list ok
  for B1 if needed to wire end-to-end; final scenes land in B2).
Register in the same place setup.ts is registered; help text: "detect-first provider
onboarding (differs from setup: finds what you already have)".

## Tests
packages/coding-agent/test/onboard-scan.test.ts (or repo test convention — CHECK how existing
coding-agent tests run: bun test? vitest? Match exactly):
- ranking order incl Arnold-first; ready/candidate/missing logic; foreign store candidates;
  no-secret-values assertion over serialized output; defaultModel null-safe.
Non-TTY command test per above.
Run: repo's actual test runner for coding-agent package + typecheck (find exact scripts in package.json).
Report verbatim outputs + deviations.
