# Executor brief — B2 (scenes: UX structure per W1/K2)
North Star: house style indistinguishable from stock wizard; Enter-only happy path; one verified route; persist-once.
Worktree /tmp/oh-my-pi-onboard-ui. Commit NOTHING. Replace the B1 placeholder scenes.ts wholesale.

## Spike FIRST (30 min cap, record in .oracle/briefs/b2-spike.md)
Q1: how scenes reach AuthStorage/modelRegistry — read how runSetupWizard constructs SetupSceneHost.ctx and what commands/setup.ts passes; determine the correct way for onboard scenes to obtain authStorage + modelRegistry + settings (standalone construction vs session ctx). Q2: strict single-provider verify shape — find checkCredentials usage (auth-gateway-cli.ts) and decide in-process call vs child `omp -p`; prefer in-process if it returns actionable health without spawning.

## Scene 1 — detect-pick
- SelectList over scanProvidersLive() entries, found-first ranking already from scan.ts:
  ready rows prefix green-check glyph per house theme conventions (STUDY existing SelectList
  item styling/theme usage in model.ts/sign-in.ts — match exactly, do NOT invent glyphs),
  candidate rows neutral marker, missing rows dimmed/hidden by default behind a
  "Show all providers…" trailing list item (house pattern: theme.ts 'Browse all…' fallback).
- Recommended entry marked with the same suffix convention stock lists use; preselected.
- Empty state [U1]: zero detections → render inline text block ("No providers detected." +
  next-step line) instead of instantiating SelectList; Enter opens manual api-key wire path;
  Esc exits cleanly (exit code contract: 0 only after verified route; Esc = decline exit 1;
  non-TTY handled upstream exit 2).
- Copy tone [U4]: imperative title "Set up a provider", subtitle ending in what happens next,
  matching providers.ts/model.ts patterns exactly.

## Scene 2 — wire+verify (single scene)
- Route by selected entry's auth kind:
  api_key → Input(mask=true), onSubmit persists via AuthStorage API used by login/paste-code
  paths (find the canonical upsert call); oauth-capable → reuse sign-in flow machinery
  (SignInTab/OAuthSelectorComponent internals or the login lifecycle it drives);
  cli_proxy (grok) → inlined models.yml generation: merge-preserving textual splice or full
  YAML rewrite fallback EXACTLY like the Arnold Python implementation semantics (preserve
  unknown fields/other providers, atomic write via rename in same dir, $HOME-expanded helper
  path, copy grok-token.py equivalent script into agent dir) — port logic, don't reimplement
  differently.
- Default model auto-committed from RECOMMENDED_MODELS (Enter-only happy path): show as a
  confirmation line inside scene 2 ("Route: <provider>/<model>"), NOT a separate scene.
- Strict verify [U2]: chosen approach from spike; spinner glyph frames on SETUP_TICK_MS with
  elapsed seconds; failure → inline error line + loop back into this scene's input for same
  provider (max 3 attempts) then offer different-provider escape; never claim success without
  verify pass.
- Success → renderSetupOutro reuse (existing outro), copy adapted minimally.
- Footer hints [U5] scene-specific; splash fires automatically via overlay phases [U3] —
  verify in pty smoke that intro animation plays before scene 1.

## Tests
- bun test files matching repo conventions: scan→scene mapping pure functions (visible-window/
  marker assignment), wire models.yml generation unit tests (merge/idempotence/atomicity/$HOME)
  using temp dirs, non-TTY paths.
- pty smoke: scripted keystrokes (bun + node-pty? check devDeps; else use script(1) or python
  pty harness) driving happy path end-to-end against a sandboxed PI_CODING_AGENT_DIR/HOME;
  capture transcript to .oracle/evidence/b2-pty-transcript.txt; assert intro animation frames
  present, Enter-only count <=2 prompts on happy path, no secret echo.
Run: bun test <files> + bun run check:types. Report verbatim tails + spike findings + deviations.
